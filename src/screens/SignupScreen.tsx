import React from 'react';
import {Button, SafeAreaView} from 'react-native';

const SignupScreen = () => {
  return (
    <SafeAreaView>
      <Button title="회원가입" />
    </SafeAreaView>
  );
};

export default SignupScreen;
