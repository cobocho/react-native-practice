import React from 'react';
import {Button, SafeAreaView} from 'react-native';

const LoginScreen = () => {
  return (
    <SafeAreaView>
      <Button title="로그인" />
    </SafeAreaView>
  );
};

export default LoginScreen;
