import React from 'react';
import {Button, SafeAreaView} from 'react-native';

const MapScreen = () => {
  return (
    <SafeAreaView>
      <Button title="지도" />
    </SafeAreaView>
  );
};

export default MapScreen;
